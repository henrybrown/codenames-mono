/** Public exports for the lobby state layer — the aggregate loader. */
export { createLobbyAggregateLoader } from "./load-lobby-aggregate";
export type { LobbyAggregateLoader } from "./load-lobby-aggregate";
